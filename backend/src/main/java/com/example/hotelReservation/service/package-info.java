/**
 * This package contains the service layer of the Hotel Reservation application.
 * <p>
 * The service layer is responsible for implementing the business logic
 * and acts as an intermediary between the controller layer and the
 * repository layer.
 * It processes input data, applies business rules, and interacts with the
 * data access layer.
 * </p>
 */
package com.example.hotelReservation.service;
